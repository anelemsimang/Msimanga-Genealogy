# Next.js app is in /lineage.
# Publish must be ".next" (relative to base), NOT "lineage".
# If the Netlify UI still shows publish = lineage, this file overrides it.
[build]
  base = "lineage"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
